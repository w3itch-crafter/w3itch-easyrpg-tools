---
kind: pipeline
type: docker
name: default

clone:
  disable: true

steps:
  - name: clone
    image: alpine/git
    commands:
      - git clone https://github.com/EasyRPG/Tools.git
  - name: build-gencache
    image: ubuntu:20.04
    commands:
      - sudo apt update
      - sudo apt install build-essential
      - cd ./Tools/gencache
      - ./bootstrap # only needed if using a git checkout
      - ./configure
      - make
---
kind: signature
hmac: f7146c9dbbd3cd62e660e29ad67673fee9a93c124384a08ed7976ca104872e51

...
