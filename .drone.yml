---
kind: pipeline
type: docker
name: default

clone:
  disable: true

steps:
  - name: clone
    image: alpine/git
    commands:
      - git clone https://github.com/EasyRPG/Tools.git
  - name: build-gencache
    image: ubuntu:20.04
    commands:
      - apt install build-essential
      - cd ./Tools/gencache
      - ./bootstrap # only needed if using a git checkout
      - ./configure
      - make
---
kind: signature
hmac: 921054b034767f499db491436a9a9c0edde749e78e019058b6a57bbed2a1eb9e

...
