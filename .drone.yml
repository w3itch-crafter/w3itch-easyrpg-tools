---
kind: pipeline
type: ssh
name: ssh-deploy-prod

clone:
  disable: true

server:
  host: api.w3itch.io
  user: w3itch
  ssh_key:
    from_secret: SSH_ID_RSA

steps:
  - name: build-gencache
    commands:
      - git clone https://github.com/EasyRPG/Tools.git
      - cd ./Tools/gencache
      - ./bootstrap # only needed if using a git checkout
      - ./configure
      - make
      - chmod +x ./gencache
      - mv /var/easyrpg/gencache/bin/gencache /var/easyrpg/gencache/bin-bak/
      - cp ./gencache /var/easyrpg/gencache/bin/
trigger:
  branch:
    - main
  event:
    - push

---
kind: signature
hmac: 9bf4bcd3d9d59ca279bc861e76f4a8902ca8bc6cd197cebe60b61b3a15907c4f

...
